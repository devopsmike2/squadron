# govulncheck reachable-vulnerability allowlist (Squadron OSS)
#
# The govulncheck CI gate (scripts/govulncheck-gate.sh) FAILS on any REACHABLE
# (call-graph-reachable) Go vulnerability whose OSV ID is NOT listed here.
# Add an entry ONLY for a vuln that is reachable today and consciously deferred,
# always with a TODO to remove it once fixed. NEW reachable vulns are RED by design.
#
# Format: one OSV ID per line. `#` starts a comment. Blank lines ignored.
#
# ---------------------------------------------------------------------------
# (empty) — no reachable vulnerabilities are currently deferred.
#
# GO-2026-5158 (otel baggage DoS) was resolved by the otel@1.44.0 bump that rode
# in with the grpc@v1.83.2 upgrade (GO-2026-6348/6443 fix); govulncheck no longer
# reports it as reachable, so its allowlist entry was removed.
